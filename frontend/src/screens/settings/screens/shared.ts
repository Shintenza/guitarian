import { StyleSheet } from "react-native-unistyles";

export const sharedStyles = StyleSheet.create({
  container: {
    gap: 12,
    padding: 12,
  },
  header: {
    flexDirection: "row",
    gap: 8,
    alignItems: "center",
  },
});
